extend type Mutation {
  """
  Creates a new booking record.
  """
  insertBooking(
    id: String!,
    providerId: String!,
    clientId: String!,
    serviceIds: [String]!,
    date: Timestamp!,
    status: String!,
    isPaid: Boolean!
  ): Booking
}
