
type User @table(name: "users") {
  id: String! @pk
  name: String!
  email: String! @unique
  role: String!
  createdAt: Timestamp!
}

extend type Mutation {
  insertUser(
    id: String!
    name: String!
    email: String!
    role: String!
  ): User! @permission(admin: true)
}
