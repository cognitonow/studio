
extend type Query {
  # Note: Standard practice is to not expose a 'listUsers' query publicly
  # for security reasons. Getting a user should be specific.
  getUser(id: String!): User
}

extend type Mutation {
  insertUser(
    id: String!
    name: String!
    email: String!
    role: String!
  ): User
}
