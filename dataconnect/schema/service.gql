extend type Query {
  listServices: listServicesResponse
}

type listServicesResponse {
  services: [Service!]!
}

type Service {
  id: ID!
  categoryId: String!
  name: String!
  description: String!
  price: Float!
  duration: Int!
  provider: [ServiceProvider!]
}
